{
  "name": "functions-auth",
  "description": "Cloud Functions for Firebase - Auth Triggers (Gen-1)",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions:auth-gen1",
    "shell": "firebase functions:shell --codebase auth-gen1",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions:auth-gen1",
    "logs": "firebase functions:log --codebase auth-gen1"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0"
  },
  "private": true
}
